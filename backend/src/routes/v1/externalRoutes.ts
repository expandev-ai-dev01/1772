import { Router } from 'express';

const router = Router();

// No external routes are defined in the base structure.
// This is where public endpoints like registration or login would go.

export default router;
